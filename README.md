# choicemovie

## env

```console
$ python3 -V
Python 3.7.3
$ cd choicemovie
$ python3 -m venv venv
$ . venv/bin/activate
(venv) $ pip install --upgrade pip
(venv) $ pip install -r requirements.txt
```
